import styled from 'styled-components/native'

export const BackButtonContainer = styled.TouchableHighlight`
  align-self: flex-end;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  border-radius: 1000px;
  padding: 10px;
  position: absolute;
  align-self: flex-end;

  top: 90px;
  left: 7%;
`
